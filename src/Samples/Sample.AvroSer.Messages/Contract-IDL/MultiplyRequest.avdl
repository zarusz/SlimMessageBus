@namespace("Sample.AvroSer.Messages")

protocol SampleProtocol {

	record MultiplyRequest {
		string OperationId;
		int Left;
		int Right;
	}

	record MultiplyResponse {
		string OperationId;
		int Result;	
	}

}